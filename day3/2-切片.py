# 针对字符串切片
s = "hello,world"
# 获取索引为1到5的字符
s_ = s[1:5]
print(s_)  # 输出 "ell"

# 操作列表
lst = [1, 2, 3, 4, 5]
# 获取索引为1到3的元素
lst_ = lst[1:4]
print(lst_)  # 输出 [2, 3, 4]

# 操作元组
tpl = (1, 2, 3, 4, 5)
# 获取索引为1到3的元素
tpl_ = tpl[1:4]
print(tpl_)  # 输出 (2, 3, 4)
